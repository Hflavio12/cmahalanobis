% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmahalanobis.R
\name{cspearman}
\alias{cspearman}
\title{Calculate the Spearman correlation or dissimilarity for each pair of factors or for the index.}
\usage{
cspearman(
  dataset,
  formula,
  plot = TRUE,
  min_group_size = 3,
  method = "average",
  max_index_sample = NULL,
  automatic_encoding = FALSE,
  na_removal = FALSE,
  grouping_stat = TRUE,
  dissimilarity = TRUE,
  t_test = TRUE
)
}
\arguments{
\item{dataset}{A dataframe.}

\item{formula}{The index of the dataframe, otherwise a variable or variables (two or more) with factors which you want to calculate the Spearman correlation or dissimilarity matrix or matrices (two or more).}

\item{plot}{Logical, if TRUE, dendrograms with various agglomeration metrics for factors (two or more) are displayed. With "index" in formula, a dendrogram considering the observation is displayed.}

\item{min_group_size}{Minimum group size to maintain. The default value is 3, therefore groups, inside variables, with less than 3 observations will be discarded. For "index", this value is always 1.}

\item{method}{The agglomeration method for calculating dissimilarities between observations. Available methods are "ward.D", "ward.D2", "single", "complete", "average", "mcquitty", "median" or "centroid".}

\item{max_index_sample}{A number of random samples from a dataset for which you want to calculate the statistic for index mode, useful with very large dataset.}

\item{automatic_encoding}{Logical, if TRUE, names inside factor variables will be transformed in numbers with ordinal order (1,2,....).}

\item{na_removal}{Logical, if TRUE, missing value removal on rows is performed.}

\item{grouping_stat}{Logical, if TRUE, the Spearman correlation or dissimilarity between factors is printed.}

\item{dissimilarity}{If TRUE, default, the dissimilarity is displayed.}

\item{t_test}{If TRUE, statistical significance using the t-student distribution is performed.}
}
\value{
According to the option chosen in formula, with "index" the Spearman matrix will be printed; instead, by specifying variables, the Spearman correlations matrix or matrices (two or more) between each pair of groups and, optionally, the plot or plots (two or more) will be printed.
}
\description{
This function takes a dataframe and a variable or variables (two or more) in input, and returns a matrix or matrices (two or more) with the Spearman correlation or dissimilarity about the factors inside them. You can also select "index" to calculate this statistic between each row.
}
\note{
If "index" is selected with variables, only correlations between rows are calculated. Therefore, this snippet: "cspearman(mtcars, ~am + carb + index)" will print correlations only considering "index". Optionally, rows with NA values can be omitted and negative values are transformed in 0.
}
\examples{
# Example with the CO2 dataset

table(CO2$Plant)

cspearman(CO2, ~Plant, 
   plot = TRUE, 
   grouping_stat = 'median', 
   automatic_encoding = TRUE)

# Example with the airquality dataset

summary(airquality)

cspearman(airquality, ~index, 
   plot = TRUE, 
   na_removal = TRUE, 
   max_index_sample = 40)
}
